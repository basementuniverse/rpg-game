export enum ContentItemType {
  Image = 'image',
  Sound = 'sound',
  Font = 'font',
  JSON = 'json',
}

export enum StateTransitionType {
  In,
  Out,
  None,
}

export enum Key {
  Up = 'ArrowUp',
  Down = 'ArrowDown',
  Left = 'ArrowLeft',
  Right = 'ArrowRight',
  Enter = 'Enter',
  Shift = 'Shift',
  Control = 'Control',
  Escape = 'Escape',
}

export enum Anchor {
  TopLeft,
  TopCenter,
  TopRight,
  CenterLeft,
  Center,
  CenterRight,
  BottomLeft,
  BottomCenter,
  BottomRight,
}
