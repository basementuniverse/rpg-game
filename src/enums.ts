export enum StateTransitionType {
  In,
  Out,
  None
}

export enum MouseButton {
  Left,
  Middle,
  Right
}

export enum Key {
  Up = 'ArrowUp',
  Down = 'ArrowDown',
  Left = 'ArrowLeft',
  Right = 'ArrowRight'
}
